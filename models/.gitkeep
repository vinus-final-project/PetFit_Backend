# 탐지 모델 가중치를 두는 곳이다.
#
# .gitignore 가 models/*.pt 를 제외하므로 가중치 자체는 커밋되지 않는다.
# 이 파일은 디렉터리를 저장소에 남겨 두기 위한 것이다. 없으면 각자 만들어야 하고,
# 만들지 않으면 YOLO_MODEL_PATH 경로에서 실패한다.
#
# 가중치는 ultralytics 가 처음 실행할 때 자동으로 내려받는다.
