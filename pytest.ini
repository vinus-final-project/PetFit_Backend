[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers

# 비동기 테스트(서비스·API 계층) 추가 시 아래를 해제한다.
# 해제하려면 pytest-asyncio 가 설치되어 있어야 한다.
# asyncio_mode = auto
