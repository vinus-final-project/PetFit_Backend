[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers

# 팀 표준 버전. 다른 버전이면 conftest.py 가 경고한다.
required_plugins = pytest-asyncio

# 비동기 테스트는 pytest-asyncio 가 자동 처리한다.
asyncio_mode = auto
