# PetFit Backend dependencies
# Python 3.10+
#
#   pip install -r requirements.txt
#
# NOTE: Keep this file ASCII-only.
# Older pip (< 23) decodes requirements files using the system locale
# (cp949 on Korean Windows) and fails on non-ASCII characters.
#
# Development and test packages are included below.
# Comment out the "dev and test" section for production deployment.

# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9      # required for multipart/form-data video upload

# --- Settings ---
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# --- Database (MySQL 8.0) ---
sqlalchemy>=2.0
aiomysql>=0.2.0              # pure-python async driver, installs without a compiler on Windows
cryptography>=42.0           # required for MySQL 8.0 default auth (caching_sha2_password)
# asyncmy>=0.2.9             # faster, but needs a C compiler; consider for Linux deployment

# --- Dev and test ---
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27                  # FastAPI TestClient
